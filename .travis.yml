---
language: node_js
node_js:
  - "6.0"
after_success:
  - npm run coverage
  - git config remote.origin.fetch "+refs/heads/*:refs/remotes/origin/*"
  - git fetch --unshallow --tags
  - git checkout docs
  - npm install
  - ./deploy.sh
sudo: false
cache:
  directories:
    - node_modules
