import { Action, VERSION } from 'denali';
// TODO verify all denali imports are actually exported

export default Action.extend({

  respond() {
    this.response.json({
      message: 'Welcome to Denali!',
      version: VERSION
    });
  }

});
