export default function databaseInitializer(/*application*/) {
  // Initializers are run during application startup. You can use them to
  // setup your app, including things like establishing connections to databases
}
